% !TEX root = ../main.tex
\chapter{Metodologia, Architettura e Principi di Design}\label{chap:methodology}

\textit{APIGuard Assurance} è un tool di security assessment automatizzato per \gls{API} REST esposte tramite gateway, progettato per essere applicazione-agnostico, contract-driven e deterministicamente riproducibile. Nasce dalla necessità di superare la frammentazione degli approcci manuali e target-specifici: strumenti che richiedono di essere riscritti per ogni nuovo target, che producono risultati non confrontabili tra esecuzioni, e che non possono essere integrati in processi di verifica continuativi. Il suo obiettivo è produrre un assessment sistematico, tracciabile e ripetibile, applicabile a qualsiasi \gls{API} REST documentata senza modifiche al codice. La sua presentazione segue un approccio \textit{top-down}: dai principi generali di sistema che ne definiscono il perimetro fino alle scelte implementative che li concretizzano.

% ─────────────────────────────────────────────────────────────────────────────
\section{Principi Fondamentali: il Perché delle Scelte Architetturali}%
\label{sec:principi-fondamentali}
% ─────────────────────────────────────────────────────────────────────────────

In coerenza con la progressione \textit{top-down} dichiarata, l'analisi ha inizio dal livello di massima astrazione: le decisioni che definiscono il carattere del tool prima ancora di descriverne i componenti. I principi discussi in questa sezione costituiscono la struttura portante del sistema: le scelte architetturali trattate nelle sezioni successive si stratificano su di essi, ciascuna risolvendo una frizione specifica all'interno dei vincoli che questi principi fondamentali stabiliscono.


\subsection{Agnosticismo Applicativo}\label{subsec:agnosticismo}

\textit{Quali target applicativi è possibile valutare senza modificare il codice del tool?}

Tutta la conoscenza del target viene derivata a runtime da due sole sorgenti: il file \texttt{config.yaml}, che raccoglie i parametri operativi del deployment (descritti in dettaglio nella Sezione~\ref{subsec:config-driven}), e la specifica OpenAPI del target, che fornisce la topologia completa degli endpoint, la definizione dei parametri di path, query e header per ciascuna operazione, gli schemi dei payload di richiesta e risposta, e i codici di stato attesi. Ne consegue un vincolo strutturale preciso: APIGuard Assurance non contiene nessun riferimento hardcoded a path, strutture dati o comportamenti di un'applicazione specifica, e qualsiasi \gls{API} REST documentata è testabile senza modifiche al codice.

Un test che interroga la superficie di attacco derivata dalla specifica e non individua endpoint pertinenti alle proprie condizioni di esecuzione produce \texttt{SKIP}, lo stato previsto per uno scope legittimamente vuoto, distinto dall'\texttt{ERROR} che segnalerebbe un malfunzionamento interno al tool. Il principio si estende a qualsiasi requisito non soddisfatto: un tool esterno non installato, la Admin \gls{API} del gateway non raggiungibile, un insieme di endpoint privi di seed configurato; ciascuna di queste condizioni produce \texttt{SKIP} sui test che ne dipendono e lascia proseguire gli altri, a meno che il risultato non costituisca una violazione confermata di un controllo perimetrale fondamentale (come l'assenza totale di autenticazione su tutti gli endpoint esposti), caso in cui la pipeline può essere interrotta esplicitamente tramite configurazione.

La differenza tra uno script target-specifico e un tool di assessment generale non è di complessità, ma di generalità: uno script con path e strutture dati scritti nel codice funziona su quel target e solo su quello (sostituita l'applicazione, va riscritto), mentre un tool che deriva tutta la propria conoscenza dalla specifica formale del target è applicabile a qualsiasi \gls{API} REST documentata senza modifiche. Questa generalità è precisamente il confine che separa un artefatto di progetto da un contributo riutilizzabile.

Il costo accettato è la dipendenza da una specifica valida. Una specifica obsoleta o deliberatamente incompleta produce un assessment parziale senza che il sistema possa rilevarne l'incoerenza: il tool non ha modo di distinguere un'assenza di vulnerabilità da un'assenza di copertura, perché il fondamento contrattuale di cui dispone è la specifica stessa. Questa tensione costituisce il limite strutturale discusso nella Sezione~\ref{sec:limiti} e si qualifica come problema aperto nel testing contract-driven, non come difetto correggibile con intervento locale.


\subsection{La Specifica OpenAPI come Fondamento Contrattuale}\label{subsec:openapi-sot}

\textit{Quale fondamento formale governa la costruzione delle richieste e delimita la superficie di assessment?}

La specifica OpenAPI è il fondamento contrattuale dell'intera pipeline: governa la costruzione delle richieste nel rispetto dei tipi, dei formati e dei parametri dichiarati, delimita la superficie di attacco agli endpoint documentati e fornisce il criterio di valutazione primario per le risposte ricevute. Questa scelta coincide con il primo controllo pre-runtime obbligatorio identificato dal NIST SP 800-228~\cite{chandramouliGuidelinesAPIProtection2026}: i controlli REC-API-1 e REC-API-2 della Sezione~3.1.1 prescrivono che tutte le API siano descritte da una specifica formale con schema di request e response dichiarato, come condizione necessaria per qualsiasi sistema di verifica automatizzato.
Per alcune classi di garanzie, la specifica è integrata da sorgenti aggiuntive, come la configurazione del gateway per i test WHITE\_BOX (Sezione~\ref{subsec:gateway-adapter}) o il confronto tra comportamenti osservati con identità distinte per i test GREY\_BOX (Sezione~\ref{subsec:box-gradient}): sorgenti che la specifica dell'interfaccia applicativa da sola non può esprimere.

Un fuzzer che genera payload senza conoscere il contratto affronta un ostacolo strutturale prima ancora di raggiungere la logica applicativa: una quota sostanziale delle richieste viene respinta con \texttt{HTTP~400} dal layer di validazione dell'input, perché i tipi, i formati e i vincoli enumerati nello schema non sono rispettati.
Questo è il limite strutturale del fuzzing cieco discusso nella Sezione~\ref{subsec:dast-sast}: lo spazio di tutti i payload sintatticamente possibili è vastissimo, ma lo spazio dei payload strutturalmente validi rispetto al contratto è definito e navigabile, come dimostrato da RESTler~\cite{atlidakisRESTlerStatefulREST2019a} nel fuzzing stateful delle \gls{API} \gls{REST}. Usare la specifica come oracolo formale abbatte questa barriera: ogni richiesta è costruita nel rispetto dei vincoli dichiarati, spostando il punto di osservazione dalla sintassi alla semantica.
La letteratura ha già dimostrato che usare la \gls{OAS} come base per un insieme strutturato di proprietà verificabili va significativamente oltre i limiti del solo oracolo HTTP~500~\cite{schemathesisDerivingSemanticsawareFuzzers2022}; il problema dell'oracolo nel contesto del security testing, e il modo in cui APIGuard lo affronta con sorgenti multiple, è discusso nella Sezione~\ref{subsec:oracle-design}.

Ne derivano due capacità operativamente distinte rispetto al fuzzing cieco. In primo luogo, la superficie di attacco viene delimitata con precisione: gli endpoint documentati dalla specifica costituiscono l'insieme di ciò che il contratto dichiara raggiungibile, mentre qualsiasi endpoint risponda senza essere incluso in quella lista è, per definizione, una Shadow API. In secondo luogo, le risposte ricevute vengono valutate rispetto a ciò che il contratto prevede per quella richiesta, non rispetto a euristiche generiche: un campo sensibile restituito in risposta a una richiesta autenticata è un finding solo se lo schema di risposta contrattuale non lo dichiarava atteso.


Anche questa proprietà porta con sé una dipendenza dalla qualità della specifica, già enunciata per la Sezione~\ref{subsec:agnosticismo}, ma con una conseguenza distinta. Lì il problema era di copertura: una specifica incompleta riduce gli endpoint osservabili senza che il tool possa rilevarlo. Qui il problema è di affidabilità dei verdetti: una specifica contrattualmente errata produce valutazioni fondate su un oracolo sbagliato. Le implicazioni di questa dipendenza sulla correttezza dell'oracolo e il suo inquadramento come limite strutturale del paradigma contract-driven sono analizzate di seguito.


\subsection{Il Problema dell'Oracolo nel Contract-Driven Security Testing}%
\label{subsec:oracle-design}

\textit{Come si stabilisce automaticamente se il comportamento osservato del sistema rispetta le garanzie di sicurezza dichiarate?}

La generazione automatizzata di input verso il sistema è un problema in larga parte risolto dalla letteratura; stabilire automaticamente se il comportamento osservato è corretto rimane il collo di bottiglia strutturale dell'automazione del testing. Barr et al.~\cite{barrOracleProblemSoftware2015} formalizzano questa sfida nella definizione di oracolo di test come funzione parziale \(D: T_A \to \mathbb{B}\) che mappa una sequenza di attività a un verdetto. Nella tassonomia proposta nel survey, le tecniche di automazione si distribuiscono in quattro categorie: \emph{specified} (oracoli ricavati da specifiche formali), \emph{derived} (oracoli inferiti da artefatti quali esecuzioni precedenti o versioni alternative del sistema), \emph{implicit} (anomalie universali come crash e segfault, rilevabili senza conoscenza del dominio) e \emph{no-oracle} (strategie per ridurre il costo dell'intervento umano in assenza di oracolo automatizzabile).


La maggior parte degli approcci sul testing delle \gls{API} REST, descritti nella Sezione~\ref{subsec:contract-driven}, opera nel dominio della conformità funzionale; la verifica esplicita delle garanzie di sicurezza rimane l'ambito strutturalmente meno sviluppato~\cite{golmohammadiTestingRESTfulAPIs2023}.

APIGuard adotta \emph{specified oracles} definiti a priori per ogni singola verifica, ricavati dalla conoscenza del dominio di sicurezza del controllo da eseguire. L'approccio metodologico è uniforme: si studia il tipo di verifica da condurre, si identificano le condizioni di fallimento rilevanti, e si codifica l'oracolo nella logica della classe di test. L'oracolo può essere ricavato dallo schema contrattuale dichiarato nella \gls{OAS}, confrontando ogni risposta con tipo, formato e campi attesi; può derivare dagli standard tecnici pubblicati, verificando il rispetto dei requisiti di conformità protocollare; oppure può essere costruito sulla base del controllo specifico da eseguire, come il confronto tra risposte ottenute con credenziali di ruolo distinto per la verifica dell'autorizzazione, o l'ispezione della configurazione del gateway per le garanzie infrastrutturali.
Ogni test incapsula il proprio oracolo come logica di classe: una volta definito quel criterio, il tool lo applica deterministicamente a qualsiasi sistema che esponga la \gls{OAS}.
L'oracolo fisso garantisce che lo stesso sistema, interrogato nella stessa configurazione, produca sempre il medesimo verdetto: è questa proprietà a rendere l'assessment confrontabile tra esecuzioni successive e a fondare empiricamente la riproducibilità deterministica discussa nella Sezione~\ref{subsec:reproducibility}.
Consegue da questa scelta un limite strutturale: la correttezza dei verdetti è condizionale alla correttezza del contratto di riferimento, come discusso nella Sezione~\ref{subsec:limite-ground-truth}.


\subsection{Config-Driven Development}\label{subsec:config-driven}

\textit{Esiste un'unica fonte di verità per tutti i parametri che governano il comportamento del tool?}

Stabilito che la conoscenza del target risiede nella specifica OpenAPI, rimane da definire il meccanismo che governa l'esecuzione. Tutti i parametri operativi risiedono nel file \texttt{config.yaml}, strutturato in aree funzionali con responsabilità distinte: la localizzazione del gateway e della specifica OpenAPI; i parametri di esecuzione globali, tra cui soglia di priorità minima, strategia di filtraggio dei test, timeout per singola esecuzione e formato del logging; il dizionario \texttt{path\_seed}, che associa a ogni parametro di path parametrico un valore concreto presente sul target; le configurazioni specifiche per singolo test, organizzate per dominio; e i parametri degli strumenti di analisi esterni, con abilitazione per tool, versione attesa e opzioni di invocazione. Le credenziali di autenticazione, per loro natura non versionabili, risiedono in un file \texttt{.env} separato, il cui contenuto viene interpolato dal loader prima del parsing. Il codice sorgente non contiene nessun literal decisionale hardcoded: qualsiasi parametro che possa variare tra un'esecuzione e l'altra è dichiarato esplicitamente e validato prima dell'avvio della pipeline.

La separazione tra logica e configurazione assegna a ogni variazione di comportamento un'unica origine dichiarata: il file \texttt{config.yaml} attivo durante quella run. Se il comportamento del tool cambia tra un'esecuzione e l'altra, è perché è cambiata la configurazione; se la configurazione è identica, l'output è identico. Questa centralizzazione è anche la condizione che rende possibile la proprietà discussa nella \hyperref[subsec:reproducibility]{sottosezione seguente}: un sistema che legga valori da variabili d'ambiente non dichiarate, da file di stato impliciti o da sorgenti runtime non controllate non può garantire che due esecuzioni successive producano lo stesso risultato, indipendentemente dalla correttezza della logica di test. Fissare la configurazione è il primo passo necessario per fissare l'output.


\subsection{Riproducibilità Deterministica}\label{subsec:reproducibility}

\textit{Come viene garantito che l'assessment produca risultati confrontabili tra esecuzioni successive?}

Lo stesso target, nella stessa configurazione, interrogato con lo stesso \texttt{config.yaml}, deve produrre risultati identici a ogni esecuzione. La riproducibilità è il presupposto che conferisce valore dimostrativo ai risultati del Capitolo~\ref{chap:experiments}: senza di essa, ogni run è un'osservazione singola non confrontabile, e l'assessment non è verificabile da terzi.

Il determinismo è ottenuto attraverso meccanismi distinti che si completano a vicenda:

\begin{itemize}
      \item Sezione~\ref{subsec:config-driven} (Config-Driven Development): se tutti i parametri decisionali sono fissi, la pipeline non ha sorgenti di variazione endogene.
      \item Struttura del \gls{DAG}, trattata nella Sezione~\ref{subsec:dag}: un grafo aciclico orientato con risoluzione topologica deterministica produce sempre la stessa sequenza di batch, indipendentemente dall'ordine in cui i test vengono scoperti a runtime.
      \item Ordinamento lessicografico dei test all'interno di ogni batch del \gls{DAG}: l'ordinamento topologico puro non garantisce stabilità tra test pronti allo stesso livello, e senza un criterio di tie-breaking esplicito la sequenza potrebbe variare tra esecuzioni; l'ordine lessicografico risolve questa ambiguità, garantendo che la sequenza sia sempre identica a parità di input.
      \item Oracolo definito a priori per ogni verifica, come discusso nella Sezione~\ref{subsec:oracle-design}: a parità di risposta ricevuta dal sistema, il criterio di valutazione è fisso e il verdetto non dipende da generazione dinamica né da stato esterno all'esecuzione.
\end{itemize}

La garanzia di riproducibilità non riguarda ogni componente dell'output: timestamp e identificatori univoci dei singoli record di evidenza variano per natura tra esecuzioni. Ciò che deve essere invariante sono i contatori aggregati (PASS, FAIL, SKIP), i verdetti per singolo test e la distribuzione dei finding per dominio. La validazione empirica, condotta su due run indipendenti che hanno prodotto contatori aggregati byte-equivalenti (\texttt{9~PASS\,/\,7~FAIL\,/\,2~SKIP\,/\,0~ERROR\,/\,98~finding}, con \texttt{diff status per-test = 0} e delta wall-clock inferiore allo 0,3\%), è discussa nella \hyperref[sec:coverage-idempotenza]{Sezione~\ref*{sec:coverage-idempotenza}}.

















% ─────────────────────────────────────────────────────────────────────────────
\section{Architettura dell'Assessment Engine}\label{sec:architettura}
% ─────────────────────────────────────────────────────────────────────────────

I principi discussi nella sezione precedente costituiscono la base del sistema. La Figura~\ref{fig:architettura-apiguard} ne visualizza la struttura d'insieme: gli ingressi della pipeline sul lato sinistro (\texttt{config.yaml} e la \gls{OAS}) sono già stati introdotti nella Sezione~\ref{sec:principi-fondamentali}; i moduli interni dell'Engine al centro, ovvero il DAG Scheduler, la separazione di contesto e l'Evidence Store, sono trattati nelle sottosezioni che seguono; l'infrastruttura target a destra, Kong Gateway con Forgejo come applicativo sottostante, è descritta nel suo deployment concreto nella Sezione~\ref{sec:testbed}.
Le scelte di questa sezione si aggiungono ai principi fondamentali, ciascuna rispondendo a un'esigenza architetturale specifica che questi ultimi lasciano aperta.

\input{assets/figures/3-architettura-apiguard.tex}


\subsection{Flusso di Dipendenze Monodirezionale}\label{subsec:dep-flow}

Il grafo delle dipendenze tra strati è strettamente aciclico e orientato in una sola direzione: il nucleo del sistema è importato dagli strati di integrazione, che sono importati dagli strati di test, che sono importati dall'orchestratore. Nessun modulo può importare da un livello superiore nella gerarchia. In termini operativi: modificare un test non può rompere il nucleo; aggiungere un connector non può influenzare l'orchestratore; l'intera base del nucleo può essere testata unitariamente senza istanziare alcun client \gls{HTTP} né alcun test di sicurezza.

In assenza di un vincolo esplicito sulla direzione delle dipendenze, la pressione evolutiva naturale del codice tende verso cicli: un test che trova conveniente importare un'utility da un altro test, un connector che ha bisogno di un modello definito nell'orchestratore, e così via. La direzione è unidirezionale: un test può dipendere dal nucleo per accedere ai componenti condivisi, ma nessun modulo del nucleo può dipendere da un test. Ogni ciclo rende un modulo impossibile da isolare, e un modulo non isolabile non è testabile né sostituibile. Il vincolo è verificabile per ispezione del grafo di importazione: nessun modulo dello strato di test importa componenti degli strati superiori, e ogni strato dichiara esplicitamente nel proprio contratto le sole dipendenze che gli sono ammesse.

La struttura a strati rende praticabile uno degli sviluppi futuri descritti nella Sezione~\ref{sec:sviluppi-futuri}: un modulo di test contribuito da terze parti viene scoperto automaticamente\footnote{La scoperta automatica non è priva di vincoli: un modulo viene incluso nella pipeline solo se rispetta i contratti del tool, ovvero se dichiara tutti gli attributi obbligatori e si colloca nella directory attesa. Un modulo che non soddisfi questi requisiti viene ignorato silenziosamente oppure produce un errore di validazione.} senza che nessuna modifica al nucleo del sistema sia necessaria, perché il nucleo non contiene nessun riferimento ai test che lo utilizzano.


\subsection{Scheduling Basato su \glsentrytext{DAG} e Orchestrazione Deterministica}\label{subsec:dag}

L'ordine di esecuzione dei test è calcolato a partire dalle dipendenze che ogni test dichiara esplicitamente come parte del proprio contratto. Lo scheduler raccoglie queste dichiarazioni, costruisce un \gls{DAG} orientato e risolve l'ordine topologico, producendo una sequenza di batch: gruppi di test il cui insieme di dipendenze è già stato soddisfatto. I batch sono sequenziali tra loro; all'interno di ogni batch i test sono privi di dipendenze reciproche. La traduzione di questo schema in codice, incluse le strutture dati e le librerie impiegate, è descritta nella Sezione~\ref{subsec:dag-impl}.

Il problema che questa struttura risolve non è solo di ordinamento, ma di correttezza semantica. Un test che verifica la validità crittografica di un token \gls{JWT} presuppone che il meccanismo di autenticazione funzioni: se eseguito prima del test di autenticazione, il token non è disponibile nel contesto condiviso e il test fallirebbe per la ragione sbagliata. Senza una dichiarazione esplicita della dipendenza, la disponibilità del token è una precondizione implicita che dipende dall'ordine di esecuzione scelto: un cambio di scheduling la viola silenziosamente. Con la dipendenza dichiarata, il \gls{DAG} verifica la precondizione a monte dell'esecuzione, e un token non disponibile diventa un errore rilevabile prima che un singolo test venga avviato.

Lo scheduler include due proprietà di correttezza strutturale:

\begin{itemize}
      \item Rilevazione statica dei cicli: se il grafo delle dipendenze contiene un ciclo, l'errore viene segnalato prima che un singolo test venga eseguito, impedendo che stati inconsistenti si propaghino silenziosamente nella pipeline.
      \item Salvaguardia contro lo stallo: se il grafo risulta ancora attivo ma non riesce a estrarre nessun test pronto, il sistema emette un errore strutturato che elenca i test mai schedulati e interrompe l'esecuzione in modo controllato, fornendo all'operatore la diagnostica necessaria senza dover ricostruire lo stato interno del grafo.
\end{itemize}

L'ordinamento topologico puro non garantisce un ordine unico tra test pronti allo stesso livello: senza un criterio di tie-breaking\footnote{\textit{Tie-breaking}: regola di ordinamento applicata tra elementi a parità di livello topologico, per garantire una sequenza stabile e riproducibile tra esecuzioni.} esplicito la sequenza potrebbe variare tra esecuzioni. Lo scheduler risolve questa ambiguità ordinando lessicograficamente i test all'interno di ogni batch, convertendo la struttura parzialmente ordinata del grafo in una sequenza riproducibile, condizione necessaria per la proprietà di riproducibilità descritta nella Sezione~\ref{subsec:reproducibility}.

La struttura a batch porta con sé una proprietà che la versione attuale non sfrutta: i test all'interno di uno stesso batch non hanno dipendenze reciproche e sono concettualmente parallelizzabili. Introdurre esecuzione parallela all'interno di un batch è una modifica localizzata allo scheduler, senza impatto sul design dei singoli test, a condizione che le implicazioni di concorrenza sullo stato condiviso vengano analizzate formalmente. Questa traiettoria è documentata come sviluppo futuro nella Sezione~\ref{sec:sviluppi-futuri}; la sequenzialità attuale è una scelta deliberata di scope, non un vincolo architetturale.


\subsection{Split State e Immutabilità}\label{subsec:split-state}

Durante l'esecuzione sequenziale dei test, due categorie di informazione coesistono nel processo: ciò che si sapeva del target prima che la pipeline iniziasse, e ciò che si scopre o si produce durante l'esecuzione. Trattarle con lo stesso oggetto mutabile introduce una categoria di bug difficile da diagnosticare: un test che scrive accidentalmente sulla rappresentazione del target altera la vista condivisa da cui ogni verdetto dipende, senza che il sistema possa rilevarlo.

La soluzione è la separazione netta in due oggetti con caratteristiche opposte:

\begin{itemize}
      \item \textbf{TargetContext} - immutabile per costruzione: viene popolato una volta sola prima che la pipeline inizi e qualsiasi tentativo di modifica produce un errore immediato nel punto in cui viene effettuato. Raccoglie tutto ciò che si conosce del target prima dell'esecuzione:
            \begin{itemize}
                  \item la specifica OpenAPI dereferenziata come mappa strutturata di endpoint, parametri e schemi;
                  \item i parametri di tuning specifici per ogni test ricavati dalla configurazione;
                  \item l'adapter del gateway iniettato prima dell'avvio della pipeline;
                  \item le credenziali, risolte dal loader a partire dal file \texttt{.env} e congelate nel \texttt{TargetContext} tramite la configurazione \texttt{frozen}.\footnote{L'accesso avviene esclusivamente tramite i campi tipizzati del modello Pydantic costruito a runtime: non esiste un dizionario libero né accesso diretto ai valori grezzi.}
            \end{itemize}
      \item \textbf{TestContext} - mutabile, raccoglie ciò che la pipeline produce. L'accesso è strutturato in tre canali tipizzati con responsabilità distinte, anziché in un dizionario ad accesso libero:
            \begin{itemize}
                  \item canale dei token: organizza le credenziali per ruolo, con accesso tramite un identificatore definito dal contratto del sistema, non tramite chiavi stringa arbitrarie;
                  \item canale di teardown: mantiene un registro \gls{LIFO} delle risorse create durante la run, garantendo che il cleanup segua l'ordine inverso della creazione, come discusso nella Sezione~\ref{subsec:teardown};
                  \item canale dei dati condivisi: consente a test in batch successivi di consumare risultati prodotti da batch precedenti, senza che tra i moduli vengano introdotte dipendenze di importazione.
            \end{itemize}
\end{itemize}

L'implementazione concreta di questa separazione, inclusi i tipi e i meccanismi di accesso, è descritta nella Sezione~\ref{subsec:split-state-impl}.


Qualsiasi informazione scoperta durante l'esecuzione (endpoint presenti sul target ma non dichiarati nella specifica, token acquisiti, risorse create) appartiene per definizione allo stato dell'assessment e non alla conoscenza preliminare del target: viene quindi registrata nel contesto di esecuzione, non in quello del target immutabile. La separazione fa sì che la base informativa da cui ogni verdetto dipende non possa essere alterata durante la run, condizione necessaria perché la riproducibilità descritta nella Sezione~\ref{subsec:reproducibility} sia dimostrabile empiricamente e non solo enunciabile come proprietà attesa.


\subsection{Gateway-Agnostic Adapter}\label{subsec:gateway-adapter}

La Sezione~\ref{subsec:agnosticismo} stabilisce che la conoscenza del target applicativo deriva esclusivamente dalla specifica OpenAPI e dal \texttt{config.yaml}. La proprietà presentata in questa sezione estende lo stesso principio di agnosticismo a un livello distinto, lo strato di controllo del gateway: alcune garanzie di sicurezza, come la verifica che i timeout sui servizi upstream siano configurati, che il plugin di circuit breaking sia attivo o che le credenziali di servizio non siano hardcoded nelle variabili d'ambiente, non sono osservabili interrogando gli endpoint esposti ai client. Queste informazioni risiedono nel piano di configurazione amministrativo del gateway e richiedono un canale di accesso separato dalle normali probe \gls{HTTP}.

Il pattern scelto è un'interfaccia astratta di sola lettura verso il gateway: i test \texttt{WHITE\_BOX} vi accedono esclusivamente tramite i metodi che questa interfaccia espone, senza contenere nessun riferimento al gateway specifico installato nel deployment. L'implementazione concreta viene selezionata a runtime in base alla configurazione e iniettata nel contesto del target prima che la pipeline abbia inizio. Se la Admin \gls{API} del gateway non è configurata o non è raggiungibile, tutti i test \texttt{WHITE\_BOX} transitano a \texttt{SKIP} con motivazione esplicita, senza errori a cascata. I dettagli dell'interfaccia e dell'implementazione Kong sono descritti nella Sezione~\ref{subsec:kong-adapter-impl}.

Il vantaggio è che aggiungere supporto per un gateway diverso richiede di implementare una nuova classe concreta che soddisfi l'interfaccia, senza toccare nessuno dei test che la utilizzano: la logica di verifica non cambia, cambia solo il meccanismo con cui i dati di configurazione vengono recuperati. Il costo è che l'interfaccia può esprimere solo le verifiche comuni a tutti i gateway supportati: controlli che richiedano funzionalità vendor-specific non generalizzabili rimangono fuori dal perimetro dell'astrazione, tensione analizzata nel dettaglio nella Sezione~\ref{subsec:limite-astrazione}.


\subsection{Streaming Evidence Store}\label{subsec:evidence-store}

Un finding prodotto da un test acquista valore dimostrabile solo se accompagnato dall'evidenza \gls{HTTP} che lo supporta: la request inviata, la response ricevuta, il timestamp e gli identificatori di traccia. Senza di essa, l'esito del test è un'asserzione non verificabile da terzi, priva del requisito di non-ripudiabilità che un audit trail tecnico richiede.

Il design originale dell'Evidence Store utilizzava un buffer a capacità fissa in memoria: i record più vecchi venivano rimossi silenziosamente quando il buffer si riempiva, generando riferimenti a record inesistenti nell'audit trail. Il risultato era un trail rotto senza che il sistema producesse nessun errore visibile. Il difetto era strutturale, non correggibile aumentando la capacità: qualsiasi limite fisso avrebbe trasformato la capacità in un parametro di tuning il cui valore corretto dipende da ciò che si vuole misurare, informazione non disponibile a priori.

L'Evidence Store scrive ogni record su file locali con flush immediato, senza buffering in memoria e senza dipendenze verso database o storage remoto: ogni record è permanente nel momento in cui viene prodotto e non esiste un limite superiore al numero di record per test. Il ciclo di vita dettagliato del componente è descritto nella Sezione~\ref{subsec:evidence-lifecycle}. La scelta di operare esclusivamente su file locali è anche la condizione che abilita quanto descritto nella sottosezione seguente.


\subsection{Assenza di Dipendenze di Stato Esterno}\label{subsec:zero-state}

APIGuard Assurance non ha dipendenze di stato esterno a runtime: tutto lo stato mutabile del processo risiede in memoria o in file locali, e le uniche connessioni di rete aperte durante un run sono quelle verso il target applicativo e, opzionalmente, verso la Admin \gls{API} del gateway. Entrambe sono connessioni di test verso il sistema da valutare, non dipendenze infrastrutturali del tool. Un tool che richiede database, message broker o cache esterna trasferisce la propria affidabilità a servizi esterni: l'exit code al termine dell'esecuzione riflette lo stato dell'assessment solo se tutti quei servizi erano disponibili e corretti, una garanzia che il tool non può fornire autonomamente.


Nessun client di database, cache distribuita o \gls{SDK} di storage remoto figura tra le dipendenze obbligatorie del progetto: il deployment si riduce all'installazione del package e alla fornitura del file di configurazione. Questa assenza è anche la condizione che rende il canale di comunicazione con la pipeline, descritto nella Sezione~\ref{subsec:exit-codes}, strutturalmente affidabile: l'exit code al termine dell'esecuzione riflette esclusivamente lo stato dell'assessment, non quello di servizi accessori che potrebbero fallire in modo indipendente.



















% ─────────────────────────────────────────────────────────────────────────────
\section{Domini di Sicurezza e Box Gradient Applicato}\label{sec:domini}
% ─────────────────────────────────────────────────────────────────────────────
Le sezioni precedenti hanno definito i principi architetturali e strutturali su cui il sistema si fonda.
Questa sezione definisce cosa il sistema misura e secondo quale logica organizza le proprie misure: è il punto di convergenza tra la tassonomia delle minacce discussa nel Capitolo~\ref{chap:background}, dove le categorie di vulnerabilità sono state analizzate in quanto problema, e la struttura operativa del tool, dove quelle stesse categorie diventano il perimetro di garanzie che un assessment sistematico è tenuto a coprire.


\subsection{Tassonomia degli Otto Domini di Assurance}\label{subsec:domini-tassonomia}

La superficie di sicurezza di un'\gls{API} esposta tramite gateway non è omogenea: le garanzie che il sistema dichiara appartengono a famiglie semanticamente distinte, ciascuna con caratteristiche di verifica differenti. La tassonomia adottata organizza questa eterogeneità in otto domini di assurance, ciascuno corrispondente a una categoria di rischio specifica.
Questa categorizzazione si allinea alle categorie di rischio per le API cloud-native e ai controlli runtime identificati, rispettivamente, nella Sezione~2 e nella Sezione~3.2.1 del NIST SP 800-228~\cite{chandramouliGuidelinesAPIProtection2026}.
La Tabella~\ref{tab:domini} li riporta con il loro titolo e l'ambito di verifica che ciascuno racchiude.

\input{assets/tables/3-domini.tex}

La sequenza dei primi tre domini rispecchia una catena logica di precondizioni: non ha senso verificare l'autorizzazione (D2) senza avere prima verificato che l'autenticazione funzioni (D1), così come non ha senso verificare l'autenticazione senza conoscere la superficie di attacco (D0). Un assessment che verifichi D2 in assenza di certezze su D1 raccoglie evidenza su un sistema il cui comportamento in caso di autenticazione compromessa è ignoto. I domini D3-D7 non seguono la stessa dipendenza lineare: appartengono a famiglie semantiche distinte, ciascuna con le proprie precondizioni di accesso, senza vincoli di ordinamento reciproco tra di loro. Questa catena logica si riflette nelle dipendenze dichiarate dai singoli test che operano su questi domini, il cui meccanismo di risoluzione è descritto nella Sezione~\ref{subsec:dag}.

Il Dominio~5 occupa una posizione particolare nella tassonomia: è architetturalmente predisposto nella struttura del sistema, ma i test corrispondenti non fanno parte dell'implementazione presentata in questa tesi e sono pianificati come sviluppo futuro. La numerazione è riservata intenzionalmente per preservare la coerenza della sequenza nelle versioni successive.


\subsection{Il Box Gradient come Mapping dalle Precondizioni al Privilegio}\label{subsec:box-gradient}

La classificazione Black/Grey/White Box del testing, introdotta nella Sezione~\ref{subsec:asimmetria-box-gradient} come asimmetria informativa tra tester e sistema, viene derivata in APIGuard Assurance dalle precondizioni concrete che ogni garanzia impone al tester: non è una categoria assegnata retroattivamente ai test, ma il prodotto di ciò che ciascuna verifica richiede prima ancora di poter iniziare. Alcune garanzie sono fisicamente inaccessibili senza uno specifico livello di privilegio, e quella inaccessibilità è il criterio che determina il livello.

Il mapping adottato, sintetizzato nella Tabella~\ref{tab:box-gradient}, correla la strategia operativa alle precondizioni concrete che ogni classe di garanzie impone al tester.
Il livello di visibilità di ogni test è determinato da ciò che la verifica richiede per essere condotta correttamente.

\input{assets/tables/3-box-gradient.tex}

I controlli perimetrali applicati dal gateway a qualsiasi interlocutore, autenticato o meno, non presuppongono conoscenza dell'infrastruttura interna: il risultato atteso è lo stesso indipendentemente da chi effettua la richiesta. Eseguirli senza credenziali è l'unico modo per ottenere evidenza non contaminata da privilegi impliciti, ed è per questo che i test \texttt{BLACK\_BOX} sono la scelta naturale per questa classe di garanzie.

Un controllo di autorizzazione \gls{RBAC}, al contrario, richiede che il sistema si trovi in uno stato autenticato con almeno due identità di ruolo distinto. Senza quello stato il test non raggiunge nemmeno la logica che intende sondare: il gateway respinge la richiesta al livello di autenticazione, prima ancora che la logica di autorizzazione venga valutata, e il risultato osservato è indistinguibile da un corretto rifiuto di autenticazione. La strategia \texttt{GREY\_BOX} è la risposta a questa precondizione, riconosciuta anche in letteratura come requisito strutturale per la rilevazione automatizzata di fault di autorizzazione nelle \gls{API} REST~\cite{sahinEnhancingRESTAPI2026}.

I test \texttt{WHITE\_BOX} occupano una posizione metodologicamente distinta. Alcune garanzie sono più efficacemente verificabili tramite ispezione diretta della configurazione del gateway che tramite probe empirici verso gli endpoint: la versione \gls{TLS} supportata, la presenza e la parametrizzazione dei plugin di sicurezza, l'assenza di credenziali hardcoded nelle variabili d'ambiente. Un test empirico che cerchi di inferire questi parametri dall'esterno richiederebbe attacchi elaborati con esito incerto; un audit della configurazione li verifica in modo deterministico e riproducibile. È per questo che i test \texttt{WHITE\_BOX} delegano la lettura della configurazione all'adapter del gateway descritto nella Sezione~\ref{subsec:gateway-adapter}, anziché effettuare probe sull'interfaccia pubblica.


In contesti industriali, la disponibilità delle credenziali e dell'accesso amministrativo varia da deployment a deployment. Un'organizzazione che non dispone di accesso amministrativo al gateway può eseguire l'intera suite \texttt{BLACK\_BOX} e \texttt{GREY\_BOX}, mentre i test \texttt{WHITE\_BOX} transitano a \texttt{SKIP} con motivazione esplicita.

\subsection{Catalogo delle Verifiche di Sicurezza}\label{subsec:test-catalog}

Il tool realizza la tassonomia descritta nella Sezione~\ref{subsec:domini-tassonomia} attraverso un insieme di verifiche concrete, ciascuna corrispondente a una garanzia specifica all'interno del proprio dominio. Nella notazione seguente, \texttt{BB}, \texttt{GB} e \texttt{WB} abbreviano rispettivamente \texttt{BLACK\_BOX}, \texttt{GREY\_BOX} e \texttt{WHITE\_BOX}, come definiti nella Sezione~\ref{subsec:box-gradient}. I criteri di valutazione specifici dei test implementati sono descritti nella Sezione~\ref{subsec:test-oracles}.

\paragraph{Dominio~0: API Discovery e Inventory Management}
\begin{description}
      \item[\textbf{\texttt{0.1}}]
            \textit{Shadow API Discovery} (superato da \texttt{ext.0.1.nuclei}). \texttt{BB}, P0.\\
            Rileva endpoint attivi ma non documentati tramite fuzzing black-box su wordlist interna e confronto con la superficie contrattuale; mantenuto per compatibilità con deployment privi del connector.
            (\gls{OWASP}~API9:2023\footnote{\gls{OWASP} API9:2023: \textit{Improper Inventory Management}~\cite{OWASPTop10}.}, \gls{CWE}-200\footnote{\textit{CWE-200: Exposure of Sensitive Information to an Unauthorized Actor}.})

      \item[\textbf{\texttt{ext.0.1.nuclei}}]
            \textit{Shadow API Discovery: scansione a template.} \texttt{BB}, P0.\\
            Il tool impiega Nuclei~\cite{ProjectdiscoveryNuclei2026} come connector per rilevare esposizioni note sul target; finding di severity CRITICAL, HIGH o MEDIUM determinano \texttt{FAIL}; severity inferiore produce \texttt{PASS}.
            (\gls{OWASP}~API9:2023, \gls{CWE}-200)

      \item[\textbf{\texttt{0.2}}]
            \textit{Deny-by-Default.} \texttt{BB}, P0.\\
            Verifica che il gateway risponda \texttt{404} a path non registrati nella propria configurazione di routing, rilevando route permissive che renderebbero raggiungibili endpoint non intenzionali.
            (\gls{OWASP}~API9:2023, \gls{CWE}-284\footnote{\textit{CWE-284: Improper Access Control}.})

      \item[\textbf{\texttt{0.3}}]
            \textit{Deprecated API Enforcement.} \texttt{BB}, P0.\\
            Verifica che gli endpoint marcati \texttt{deprecated:~true} nella specifica restituiscano \texttt{410~Gone} o includano un header \texttt{Sunset}, confermando che le \gls{API} dismesse non rimangano operative oltre il ciclo di vita dichiarato.
            (\gls{OWASP}~API9:2023, \gls{CWE}-1059\footnote{\textit{CWE-1059: Incomplete Documentation}.}, \gls{RFC}~8594\footnote{\gls{RFC}~8594: \textit{The Sunset HTTP Header Field}, IETF.})
\end{description}

\paragraph{Dominio~1: Identità e Autenticazione}
\begin{description}
      \item[\textbf{\texttt{1.1}}]
            \textit{Authentication Required.} \texttt{BB}, P0.\\
            Verifica che ogni endpoint dichiarato autenticato dalla specifica respinga richieste prive di credenziali, rilevando endpoint che rispondono \texttt{2xx} in assenza di autenticazione.
            (\gls{OWASP}~API2:2023\footnote{\gls{OWASP} API2:2023: \textit{Broken Authentication}~\cite{OWASPTop10}.}, \gls{CWE}-306\footnote{\textit{CWE-306: Missing Authentication for Critical Function}.})

      \item[\textbf{\texttt{1.2}}]
            \textit{JWT Validity.} \texttt{BB}, P0.\\
            Verifica che i token \gls{JWT} presentino una firma crittograficamente valida e non siano soggetti ad algorithm confusion o token tampering.
            (\gls{OWASP}~API2:2023, \gls{CWE}-347\footnote{\textit{CWE-347: Improper Verification of Cryptographic Signature}.})

      \item[\textbf{\texttt{1.3}}]
            \textit{Credential Expiry.} \texttt{BB}, P0.\\
            Verifica che il gateway rifiuti token scaduti secondo le condizioni dichiarate nel payload \gls{JWT}.
            (\gls{OWASP}~API2:2023, \gls{CWE}-613\footnote{\textit{CWE-613: Insufficient Session Expiration}.})

      \item[\textbf{\texttt{1.4}}]
            \textit{Token Revocation.} \texttt{GB}, P2.\\
            Verifica che un token revocato esplicitamente venga immediatamente rifiutato, senza finestre di accettazione residua dopo la revoca.
            (\gls{OWASP}~API2:2023, \gls{CWE}-613, \gls{RFC}~7009\footnote{\gls{RFC}~7009: \textit{OAuth 2.0 Token Revocation}, IETF (2013).})

      \item[\textbf{\texttt{1.5}}]
            \textit{Insecure Credential Transport.} \texttt{WB}, P2.\\
            Verifica che le credenziali non transitino in chiaro, controllando redirect \gls{HTTP}$\to$\gls{HTTPS} e la presenza dell'header \gls{HSTS} con durata minima configurabile.
            (\gls{OWASP}~API2:2023, \gls{CWE}-319\footnote{\textit{CWE-319: Cleartext Transmission of Sensitive Information}.}, \gls{NIST}~SP~800-52~Rev.~2\footnote{\gls{NIST} SP~800-52 Rev.~2: \textit{Guidelines for the Selection, Configuration, and Use of Transport Layer Security (TLS) Implementations}.})

      \item[\textbf{\texttt{ext.1.5.testssl}}]
            \textit{Analisi \gls{TLS}: testssl.sh.} \texttt{BB}, P2.\\
            Il tool impiega testssl.sh~\cite{TestsslTestsslsh2026} come connector per verificare la configurazione \gls{TLS} del target rispetto ai requisiti di protocollo, cipher suite e validità del certificato.
            (\gls{OWASP}~API2:2023, \gls{CWE}-319, \gls{NIST}~SP~800-52~Rev.~2)

      \item[\textbf{\texttt{ext.1.5.sslyze}}]
            \textit{Analisi \gls{TLS}: SSLyze.} \texttt{BB}, P2.\\
            Il tool impiega SSLyze~\cite{diquetNablac0d3Sslyze2026} come connector per la stessa garanzia di \texttt{ext.1.5.testssl}, fornendo evidenza indipendente tramite ispezione programmatica dei parametri \gls{TLS}.
            (\gls{OWASP}~API2:2023, \gls{CWE}-319, \gls{NIST}~SP~800-52~Rev.~2)

      \item[\textbf{\texttt{1.6}}]
            \textit{Secure Session Management.} \texttt{WB}, P3.\\
            Verifica che i cookie di sessione presentino i flag di sicurezza obbligatori e che il session store del gateway sia configurato correttamente, prevenendo session hijacking e fixation.
            (\gls{OWASP}~API2:2023, \gls{CWE}-384\footnote{\textit{CWE-384: Session Fixation}}, \gls{CWE}-614\footnote{\textit{CWE-614: Sensitive Cookie in HTTPS Session Without \texttt{Secure} Attribute}.})
\end{description}

\paragraph{Dominio~2: Autorizzazione e Controllo degli Accessi}
\begin{description}
      \item[\textbf{\texttt{2.1}}]
            \textit{\gls{RBAC} Enforcement.} \texttt{GB}, P2.\\
            Verifica che utenti con ruolo non privilegiato non possano accedere a endpoint riservati a ruoli superiori, rilevando bypass della \gls{BFLA}.
            (\gls{OWASP}~API5:2023\footnote{\gls{OWASP} API5:2023: \textit{Broken Function Level Authorization}~\cite{OWASPTop10}.}, \gls{CWE}-285\footnote{\textit{CWE-285: Improper Authorization}.})

      \item[\textbf{\texttt{2.2}}]
            \textit{\gls{BOLA} Prevention.} \texttt{GB}, P1.\\
            Verifica che ogni utente acceda esclusivamente alle risorse di propria titolarità, rilevando accesso non autorizzato a oggetti altrui (\gls{BOLA}).
            (\gls{OWASP}~API1:2023\footnote{\gls{OWASP} API1:2023: \textit{Broken Object Level Authorization}~\cite{OWASPTop10}.}, \gls{CWE}-639\footnote{\textit{CWE-639: Authorization Bypass Through User-Controlled Key}.})

      \item[\textbf{\texttt{2.3}}]
            \textit{Destructive Operations Privilege Check.} \texttt{GB}, P1.\\
            Verifica che operazioni distruttive quali \texttt{DELETE} e \texttt{PUT} sull'intero oggetto richiedano i privilegi appropriati e non siano eseguibili da ruoli non autorizzati.
            (\gls{OWASP}~API5:2023, \gls{CWE}-285)

      \item[\textbf{\texttt{2.4}}]
            \textit{Authorization Policy Consistency.} \texttt{GB}, P1.\\
            Verifica che le policy di autorizzazione siano applicate uniformemente su tutti gli endpoint della stessa risorsa, rilevando inconsistenze tra varianti dello stesso path o tra versioni \gls{API}.
            (\gls{OWASP}~API5:2023, \gls{CWE}-285)

      \item[\textbf{\texttt{2.5}}]
            \textit{Excessive Data Exposure.} \texttt{GB}, P2.\\
            Verifica che le risposte non restituiscano campi non dichiarati nello schema contrattuale, rilevando over-fetching rispetto al contratto \gls{OAS}.
            (\gls{OWASP}~API3:2023\footnote{\gls{OWASP} API3:2023: \textit{Broken Object Property Level Authorization}~\cite{OWASPTop10}.}, \gls{CWE}-200)
\end{description}

\paragraph{Dominio~3: Integrità dei Dati}
\begin{description}
      \item[\textbf{\texttt{3.1}}]
            \textit{Input Validation.} \texttt{BB}, P2.\\
            Verifica che il sistema rifiuti payload malformati o con valori fuori dai vincoli dichiarati dalla specifica, rilevando assenza di validazione contrattuale dell'input.
            (\gls{CWE}-20\footnote{\textit{CWE-20: Improper Input Validation}.})

      \item[\textbf{\texttt{3.2}}]
            Il numero è riservato: la garanzia originariamente pianificata come test~3.2 è stata incorporata nel test~6.1 durante il design della metodologia.

      \item[\textbf{\texttt{3.3}}]
            \textit{HMAC Configuration Audit.} \texttt{WB}, P3.\\
            Verifica tramite Admin \gls{API} che il plugin \gls{HMAC} del gateway sia configurato con una finestra temporale di validazione del timestamp entro le soglie prescritte, prevenendo attacchi di replay su richieste firmate.
            (\gls{NIST}~SP~800-107~Rev.~1\footnote{\gls{NIST} SP~800-107 Rev.~1: \textit{Recommendation for Applications Using Approved Hash Algorithms}.}, \gls{CWE}-347)
\end{description}

\paragraph{Dominio~4: Disponibilità e Resilienza}
\begin{description}
      \item[\textbf{\texttt{4.1}}]
            \textit{Rate Limiting.} \texttt{BB}, P0.\\
            Verifica che il gateway applichi rate limiting ai client, restituendo \texttt{429} al superamento della soglia configurata e resistendo a tentativi di bypass tramite \texttt{X-Forwarded-For} spoofing.
            (\gls{OWASP}~API4:2023\footnote{\gls{OWASP} API4:2023: \textit{Unrestricted Resource Consumption}~\cite{OWASPTop10}.}, \gls{CWE}-400\footnote{\textit{CWE-400: Uncontrolled Resource Consumption}.})

      \item[\textbf{\texttt{4.2}}]
            \textit{Timeout Configuration Audit.} \texttt{WB}, P1.\\
            Verifica tramite Admin \gls{API} che i timeout sugli upstream del gateway siano configurati entro soglie operative, prevenendo il blocco indefinito delle connessioni in caso di backend non responsivo.
            (\gls{CWE}-400)

      \item[\textbf{\texttt{4.3}}]
            \textit{Circuit Breaker Audit.} \texttt{WB}, P1.\\
            Verifica tramite Admin \gls{API} che il gateway disponga di un meccanismo di circuit breaking o di un compensating control equivalente per la degradazione controllata del servizio.
            (\gls{CWE}-400)
\end{description}

\paragraph{Dominio~5: Visibilità e Auditing}
\begin{description}
      \item[\textbf{\texttt{5.1}}]
            \textit{Audit Logging.} \texttt{WB}, P1.\\
            Verifica che il gateway produca log strutturati per ogni richiesta con i metadata minimi per incident response e audit, con redazione dei dati sensibili.
            (\gls{NIST}~SP~800-92\footnote{\gls{NIST} SP~800-92: \textit{Guide to Computer Security Log Management}.}, \gls{ASVS}~V16.2.1\footnote{\gls{OWASP} \gls{ASVS} v5.0.0: \textit{Application Security Verification Standard}.})

      \item[\textbf{\texttt{5.2}}]
            \textit{Security Alerting.} \texttt{WB}, P2.\\
            Verifica che eventi di sicurezza anomali producano alert verso il sistema di monitoring configurato entro una latenza definita.
            (\gls{NIST}~SP~800-61~Rev.~3\footnote{\gls{NIST} SP~800-61 Rev.~3: \textit{Incident Response Recommendations and Considerations for Cybersecurity Risk Management}.}, \gls{ASVS}~V7.1.1)
\end{description}

\paragraph{Dominio~6: Configurazione e Hardening}
\begin{description}
      \item[\textbf{\texttt{6.1}}]
            \textit{Error Handling e Information Disclosure.} \texttt{BB}, P2.\\
            Verifica che le risposte di errore non espongano informazioni tecniche interne quali stack trace, versioni di framework o percorsi di file. Incorpora la garanzia originariamente pianificata come test~3.2.
            (\gls{OWASP}~API8:2023\footnote{\gls{OWASP} API8:2023: \textit{Security Misconfiguration}~\cite{OWASPTop10}.}, \gls{CWE}-209\footnote{\textit{CWE-209: Generation of Error Message Containing Sensitive Information}.})

      \item[\textbf{\texttt{6.2}}]
            \textit{Security Headers Audit.} \texttt{WB}, P3.\\
            Verifica tramite probe \gls{HTTP} che il gateway inietti nelle risposte i security header obbligatori e che gli header informativi siano assenti o privi di versione.
            (\gls{OWASP}~API8:2023, \gls{CWE}-693\footnote{\textit{CWE-693: Protection Mechanism Failure}.})

      \item[\textbf{\texttt{6.3}}]
            \textit{Gateway Hardening.} \texttt{BB}, P1.\\
            Verifica che il gateway non sia vulnerabile a \gls{HTTP} request smuggling, rilevando configurazioni che consentono a richieste con intestazioni ambigue di attraversare il proxy senza essere normalizzate.
            (\gls{OWASP}~API8:2023, \gls{CWE}-444\footnote{\textit{CWE-444: Inconsistent Interpretation of HTTP Requests}.})

      \item[\textbf{\texttt{6.4}}]
            \textit{Hardcoded Credentials Audit.} \texttt{WB}, P2.\\
            Verifica che le credenziali di servizio non siano esposte tramite endpoint di debug, né hardcoded negli \gls{URL} dei servizi upstream o nella configurazione dei plugin del gateway.
            (\gls{OWASP}~API8:2023, \gls{CWE}-798\footnote{\textit{CWE-798: Use of Hard-coded Credentials}.})
\end{description}

\paragraph{Dominio~7: Business Logic e Flussi Sensibili}
\begin{description}
      \item[\textbf{\texttt{7.1}}]
            \textit{Unsafe Consumption.} \texttt{GB}, P2.\\
            Verifica che i dati provenienti da \gls{API} di terze parti siano validati prima di essere propagati verso logiche interne.
            (\gls{OWASP}~API10:2023\footnote{\gls{OWASP} API10:2023: \textit{Unsafe Consumption of External APIs}~\cite{OWASPTop10}.}, \gls{CWE}-20)

      \item[\textbf{\texttt{7.2}}]
            \textit{\gls{SSRF} Prevention.} \texttt{GB}, P0.\\
            Verifica che il sistema non esegua richieste \gls{HTTP} verso risorse interne quando riceve un \gls{URL} come parametro di input dall'utente.
            (\gls{OWASP}~API7:2023\footnote{\gls{OWASP} API7:2023: \textit{Server Side Request Forgery}~\cite{OWASPTop10}.}, \gls{CWE}-918\footnote{\textit{CWE-918: Server-Side Request Forgery}.})

      \item[\textbf{\texttt{7.3}}]
            \textit{Race Condition.} \texttt{GB}, P2.\\
            Verifica che operazioni sensibili non siano vulnerabili a race condition tramite richieste concorrenti, rilevando inconsistenze di stato di tipo \gls{TOCTOU}.
            (\gls{OWASP}~API6:2023\footnote{\gls{OWASP} API6:2023: \textit{Unrestricted Access to Sensitive Business Flows}~\cite{OWASPTop10}.}, \gls{CWE}-362\footnote{\textit{CWE-362: Concurrent Execution Using Shared Resource with Improper Synchronization}.})

      \item[\textbf{\texttt{7.4}}]
            \textit{\gls{SSRF} Blind.} \texttt{GB}, P2.\\
            Verifica la stessa garanzia del test~7.2 nei casi in cui l'esito dell'injection non è osservabile nella risposta \gls{HTTP}, ricorrendo a un canale di osservazione out-of-band.
            (\gls{OWASP}~API7:2023, \gls{CWE}-918)
\end{description}
