% !TEX root = ../main.tex

% PATTERN DISPONIBILI IN QUESTO CAPITOLO:
%
% ALBERI DI DIRECTORY (es. struttura benchmark/dataset):
%   \dirtree{.1 root/.2 subdir/.3 file.}
%
% FIGURE AFFIANCATE CON SUBFIGURE:
%   \begin{figure}[h]\centering
%   \begin{subfigure}{\textwidth}
%       \includegraphics[width=\textwidth]{images/nome.pdf}
%   \end{subfigure}
%   \end{figure}
%
% TABELLE DI RISULTATI (con separatore | interno):
%   \begin{tabularx}{0.9\textwidth}{C | c *{3}{C}}
%   (colonna C da main.tex, pipe | tra gruppi di colonne)
%
% GRAFICI DA PDF (output da Criterion/matplotlib/R):
%   \includegraphics[width=0.8\textwidth]{images/benchmark/.../nome.pdf}


\chapter{Validazione sperimentale e risultati}%
\label{chap:experiments}

I Capitoli~\ref{chap:methodology} e~\ref{chap:implementation} hanno descritto come APIGuard Assurance è stato progettato e realizzato. La suite attiva comprende test per i domini D0, D1, D2, D3, D4, D6 e D7; il Dominio~5 (Observability) è architetturalmente predisposto ma non include test attivi in questa fase, come dichiarato nella Sezione~\ref{subsec:domini-tassonomia}. Questo capitolo porta quelle scelte architetturali a confronto con un target reale, verificando empiricamente che producano i comportamenti dichiarati.

La validazione si articola in quattro livelli con una relazione di precondizione: il testbed stabilisce le condizioni di osservazione (Sezione~\ref{sec:testbed}); la qualità ingegneristica del codebase certifica che lo strumento di misura è affidabile (Sezione~\ref{sec:release-readiness}); la copertura funzionale e l'idempotenza dimostrano che i risultati sono stabili e privi di effetti collaterali (Sezione~\ref{sec:coverage-idempotenza}); il profilo computazionale ne qualifica l'integrabilità operativa (Sezione~\ref{sec:footprint-dag}).

Tutti i dati quantitativi di questo capitolo provengono da verifiche condotte sulla versione \texttt{0.1.0} del tool: misurazioni di sistema, esecuzioni ripetute dell'assessment e ispezioni statiche del codebase, ciascuna con una procedura o un comando riproducibile riportato nel testo.

% ─────────────────────────────────────────────────────────────────────────────
\section{Topologia del Testbed Sperimentale}%
\label{sec:testbed}
% ─────────────────────────────────────────────────────────────────────────────

La scelta dell'ambiente target è derivata dalla matrice di copertura della suite: serviva un'applicazione con \gls{OAS} nativa non generata a posteriori, con endpoint protetti da autenticazione reale, almeno due livelli di privilegi distinti, endpoint parametrici con valori di path risolvibili, e comportamenti osservabili e stabili attraverso run successivi senza modifiche al target. Forgejo~14.0.3, esposto attraverso Kong~3.9 in modalità DB-less, soddisfa tutti questi requisiti. La scelta conferma inoltre in modo concreto l'agnosticismo applicativo discusso nella Sezione~\ref{subsec:agnosticismo}: il tool non contiene nessun riferimento specifico a Forgejo: cambiare target significa aggiornare i valori nel \texttt{config.yaml} (URL del gateway, percorso della specifica, path seed, credenziali), non modificare il codice.


\subsection{Composizione e Connettività dell'Ambiente di Test}\label{subsec:architettura-testbed}


L'ambiente di test è composto da tre servizi persistenti all'interno di un network Docker dedicato (\texttt{lab-net}): Forgejo~\cite{forgejoContributors2026}\footnote{Forgejo è un servizio di hosting Git self-hosted, derivato da Gitea, con \gls{API} REST documentata tramite specifica OpenAPI nativa. Espone repository, utenti, token e webhook tramite un'interfaccia \gls{HTTP} standardizzata.}~14 come applicazione target, Kong~3.9 in modalità DB-less come gateway che riceve e instrada tutto il traffico verso Forgejo, e PostgreSQL~15-alpine come backend di persistenza richiesto da Forgejo; un quarto container effimero (\texttt{forgejo-setup}) crea gli account al primo avvio e poi termina. La topologia è illustrata nella Figura~\ref{fig:testbed-topology}. Il traffico del tool parte dall'host, raggiunge Kong attraverso le porte mappate sul sistema, e Kong lo inoltra a Forgejo attraverso un upstream dichiarato nel file di configurazione \texttt{kong.yml}. L'intero ambiente è dichiarato in un singolo file Docker Compose ed eseguito su host di sviluppo, senza hardware dedicato né virtualizzazione separata. La Tabella~\ref{tab:versioni-testbed} riporta le versioni fissate.

\input{assets/figures/5-testbed-topology.tex}

\input{assets/tables/5-versioni-testbed.tex}

Kong espone tre porte sull'host: la porta~8000 per il traffico \gls{HTTP} semplice, usata dal test~1.5 per verificare il redirect verso \gls{HTTPS}; la porta~8443 per il traffico \gls{HTTPS} con certificato self-signed generato localmente, che costituisce l'endpoint primario per tutti gli altri test; la porta~8001 per la Admin \gls{API}, che fornisce i dati di configurazione ai test \texttt{WHITE\_BOX} dei Domini~3, 4 e~6. La configurazione di Kong è interamente contenuta nel file \texttt{kong.yml}, montato in sola lettura nel container: Kong legge il file a ogni avvio e opera senza database. Questo pattern, detto DB-less, è rilevante per la validazione: la configurazione è interamente nel file versionato \texttt{kong.yml} e non può essere modificata a runtime tramite l'Admin \gls{API}, il che garantisce che lo stato del gateway sia identico a ogni avvio e che le verifiche \texttt{WHITE\_BOX} interroghino sempre la stessa configurazione. Kong supporta anche la modalità con database, ma in quel caso la configurazione potrebbe essere alterata tra una run e l'altra.


\subsection{Configurazione del Testbed per la Validazione Funzionale}\label{subsec:configurazione-testbed}

Validare un tool di assessment su un ambiente completamente configurato in modo corretto produrrebbe una suite con tutti \texttt{PASS}: un risultato tautologico che non dimostra nulla sulla capacità del tool di rilevare violazioni reali. Il testbed è configurato perché alcuni test producano \texttt{FAIL} attesi e altri \texttt{PASS} attesi; la combinazione costituisce un banco di prova funzionalmente completo in cui il tool deve saper distinguere i due casi.

Le condizioni che producono \texttt{FAIL} osservabili e attesi nell'assessment sono le seguenti:

\begin{itemize}

      \item Il plugin \texttt{rate-limiting} di Kong è commentato nel file
            \texttt{kong.yml}. Il test~4.1, che verifica la presenza di un limite
            di velocità inviando richieste in loop fino a ricevere
            \texttt{HTTP~429}, registra \texttt{FAIL} con 2 finding. La
            disattivazione del rate limiting è una condizione ricorrente in
            ambienti non produttivi dove il throttling interferirebbe con i test
            applicativi, e costituisce perciò uno scenario realistico.

      \item La specifica OpenAPI di Forgejo dichiara a livello globale un
            requisito di autenticazione tramite il campo \texttt{security} del
            documento. Questo marcatore viene propagato a tutti gli endpoint
            dall'analizzatore della specifica, che li classifica come richiedenti
            autenticazione. Alcuni di quegli endpoint sono però genuinamente
            pubblici: quando il test~1.1 li interroga senza credenziali e riceve
            \texttt{2xx}, l'oracle a tre esiti della
            Sezione~\ref{subsec:probing-policy} classifica il risultato come
            \textsc{bypassed} e produce un finding. Il verdetto è \texttt{FAIL}
            con 74 finding. Il comportamento del tool è corretto: la discrepanza
            tra ciò che la specifica dichiara e ciò che il target restituisce è
            esattamente la classe di anomalia che il contract-driven testing è
            progettato per rilevare. Questo caso illustra in modo concreto la dipendenza dall'oracolo
            descritta nella Sezione~\ref{subsec:oracle-design}: quando la specifica
            dichiara un requisito che il target non applica completamente, i finding
            prodotti sono corretti dal punto di vista contrattuale ma richiedono
            che l'analista valuti se la discrepanza sia una configurazione
            intenzionale o una vulnerabilità effettiva.

      \item Il testbed usa un certificato \gls{TLS} autofirmato generato localmente
            tramite lo script \texttt{certs/gen-certs.sh}. Questa è una scelta
            deliberata: un certificato autofirmato presenta per costruzione
            configurazioni al di sotto degli standard raccomandati per la produzione,
            suite di cifratura, durata e catena di certificazione incluse, e
            costituisce uno scenario realistico per ambienti di sviluppo e staging.
            I test \texttt{ext.1.5.sslyze} e \texttt{ext.1.5.testssl} rilevano
            rispettivamente 1 e 3 finding sulla configurazione del protocollo,
            verificando che l'analisi \gls{TLS} del Dominio~1 sappia distinguere
            le singole debolezze e produrre finding specifici per ciascuna.

\end{itemize}

Le configurazioni che producono invece \texttt{PASS} attesi sono:

\begin{itemize}

      \item Il plugin \texttt{response-transformer} inietta gli header di
            sicurezza \gls{HTTP} su tutte le risposte: \texttt{Strict-Transport-Security},
            \texttt{Content-Security-Policy}, \texttt{X-Content-Type-Options} e
            \texttt{X-Frame-Options}. Il test~6.2 verifica la presenza di questi header e registra \texttt{PASS}.

      \item Il parametro \texttt{KONG\_HEADERS=off} rimuove l'header
            \texttt{Server: kong/version} da tutte le risposte. Il test~6.2,
            che verifica l'assenza di fingerprinting del gateway, registra
            \texttt{PASS}.

      \item Il servizio \texttt{http-redirect-service} produce un redirect
            \texttt{301} con header \texttt{Location} corretto per qualsiasi
            richiesta \gls{HTTP} sulla porta~8000. Il test~1.5, che verifica il
            redirect \gls{HTTP}$\to$\gls{HTTPS}, registra \texttt{PASS}.

\end{itemize}


\subsection{Struttura RBAC e Provisioning degli Utenti}\label{subsec:struttura-rbac}

Il container \texttt{forgejo-setup} crea tre account al primo avvio: \texttt{thesis-admin} (amministratore), \texttt{user-a} (utente con privilegi ordinari) e \texttt{user-b} (secondo utente non privilegiato). La struttura a tre livelli è il minimo necessario per la matrice di test: \texttt{thesis-admin} è il soggetto dei test di autenticazione che richiedono token con privilegi elevati; \texttt{user-a} è il soggetto del test~2.1 (\texttt{RBAC enforcement}), che tenta endpoint amministrativi con un token non-admin e attende \texttt{403}; \texttt{user-b} è necessario per i test di isolamento tra oggetti di utenti distinti, che verificano che le risorse di \texttt{user-a} non siano accessibili con il token di \texttt{user-b}. I test~1.4 e~2.1 dipendono da token validi prodotti dal test~1.1, dipendenza dichiarata nel \gls{DAG} come vincolo di Phase~B e discussa nella Sezione~\ref{sec:footprint-dag}.

Il provisioning è deterministico. Il container \texttt{forgejo-setup} usa l'interfaccia \gls{CLI} di Forgejo con flag \texttt{-{}-or-true}, così da non fallire se gli account esistono già da un avvio precedente.
Questa scelta realizza concretamente la proprietà di riproducibilità discussa nella Sezione~\ref{subsec:reproducibility}: il testbed raggiunge lo stesso stato iniziale indipendentemente da quante volte viene avviato, condizione necessaria perché le run successive siano comparabili.




% ─────────────────────────────────────────────────────────────────────────────
\section{Qualità della Codebase come Prerequisito di Credibilità}%
\label{sec:release-readiness}
% ─────────────────────────────────────────────────────────────────────────────

Un tool che produce verdetti di sicurezza deve poter dimostrare che la propria implementazione risponde agli stessi criteri di qualità che applica nell'analisi del target. Una codebase con errori di tipo non rilevati può produrre verdetti formalmente corretti ma basati su dati mal classificati; dipendenze con \gls{CVE} note espongono il tool stesso ad attacchi che potrebbero alterarne i risultati; simboli pubblici privi di contratto documentato rendono l'estensione da parte di terzi una fonte di errori non rilevabili staticamente. La credibilità dell'output dipende dalla qualità del codice che lo genera, e questa sezione la stabilisce prima di discutere cosa il tool ha trovato sul target.

Le verifiche coprono tre aree: l'analisi statica della codebase tramite una suite di strumenti automatizzati (Sezione~\ref{subsec:static-analysis}), la type safety a due strati che combina analisi statica con mypy e validazione a runtime con Pydantic (Sezione~\ref{subsec:dual-layer-type-safety}), e gli indicatori di qualità che riguardano documentazione interna, riproducibilità della build e portabilità dell'installazione (Sezione~\ref{subsec:production-quality}).

\subsection{Analisi Statica della Codebase}\label{subsec:static-analysis}

La suite di analisi statica della codebase è composta da cinque strumenti con responsabilità distinte e complementari, i cui risultati sono riassunti nella Tabella~\ref{tab:analisi-statica}:

\begin{itemize}
      \item \texttt{ruff}\footnote{\url{https://github.com/astral-sh/ruff}} svolge l'analisi di lint\footnote{Con \emph{lint} si intende l'analisi statica automatica del codice sorgente volta a identificare errori, violazioni di stile, pattern rischiosi e codice non conforme alle convenzioni del progetto, senza eseguire il programma.}, l'ordinamento degli import, il rilevamento di pattern insicuri e la verifica della copertura delle annotazioni di tipo.

      \item \texttt{mypy}\footnote{\url{https://mypy-lang.org}} verifica la coerenza dei tipi lungo l'intera catena di dipendenze in modalità \texttt{strict}, la configurazione più restrittiva disponibile. Il suo funzionamento è discusso in dettaglio nella Sezione~\ref{subsec:dual-layer-type-safety}.

      \item \texttt{bandit}\footnote{\url{https://github.com/PyCQA/bandit}} analizza la codebase alla ricerca di pattern associati a vulnerabilità note. I tre finding di bassa severità rilevati in \texttt{connectors/base.py} riguardano l'invocazione di \texttt{subprocess.run()} e sono soppressi con annotazione documentata: il comando è costruito esclusivamente da \texttt{ClassVar} dichiarati nel sorgente e da valori già validati da Pydantic, senza mai includere input \gls{HTTP} grezzo dall'esterno.

      \item \texttt{vulture}\footnote{\url{https://github.com/jendrikseipp/vulture}} identifica simboli pubblici definiti ma mai invocati direttamente nel codice, con soglia di confidenza all'80\%. I metodi \texttt{execute()}, \texttt{model\_*} e \texttt{validator\_*} sono esclusi tramite whitelist: \texttt{execute()} è invocato dall'engine tramite \texttt{isinstance} check, mentre i validator Pydantic vengono attivati automaticamente dal tool al momento della costruzione di ogni oggetto, senza mai comparire come chiamate esplicite nel sorgente.

      \item \texttt{pip-audit}\footnote{\url{https://pypi.org/project/pip-audit/}} verifica le dipendenze dichiarate contro i database pubblici di vulnerabilità note.
\end{itemize}

\input{assets/tables/5-analisi-statica.tex}

L'analisi statica con mypy verifica che i tipi dichiarati tra moduli siano consistenti: se \texttt{execute()} dichiara di restituire \texttt{TestResult} e un punto di chiamata la tratta come \texttt{str}, mypy lo intercetta prima dell'esecuzione. Ciò che mypy non copre è il confine con l'esterno: un file \texttt{config.yaml} caricato da disco o l'output grezzo di un connector esterno entrano nel sistema come strutture non ancora tipizzate, e nessuna verifica statica può garantirne la forma. Quel secondo livello è trattato nella Sezione~\ref{subsec:dual-layer-type-safety}.


\subsection{Dual-Layer Type Safety}\label{subsec:dual-layer-type-safety}

La type safety in un codebase Python ha due confini distinti con caratteristiche diverse. Il primo è interno: i contratti tra moduli, espressi tramite type hints, verificabili staticamente prima dell'esecuzione. Il secondo è esterno: il punto in cui dati non tipizzati provenienti dall'esterno, un file \texttt{config.yaml} caricato da disco o il raw output di un connector, entrano nel sistema. I due confini richiedono meccanismi diversi.

mypy in modalità \texttt{strict} verifica il confine interno sull'intera cartella \texttt{src/}: controlla che ogni funzione dichiari esplicitamente i tipi di ingresso e uscita (\texttt{disallow-untyped-defs}), che nessun parametro ammetta \texttt{None} implicitamente (\texttt{no-implicit-optional}), e che nessuna funzione restituisca \texttt{Any} dove è atteso un tipo concreto (\texttt{warn-return-any}). Questo include i modelli in \texttt{src/core/models/} (\texttt{TestResult}, \texttt{Finding}, \texttt{ConnectorResult}, \texttt{AttackSurface} e gli altri), le classi base dei test e dei connector, e tutta la logica dell'engine. Se durante lo sviluppo un campo di \texttt{TestResult} venisse rinominato o rimosso, ogni punto del codebase che vi accede genererebbe un errore mypy immediatamente, prima che il codice venga mai eseguito; allo stesso modo, se un tipo di ritorno dichiarato non corrisponde a ciò che la funzione restituisce effettivamente, mypy lo segnala al momento della modifica.

Pydantic~v2 presidia il confine esterno. Ogni struttura dati che attraversa il confine del sistema, dai campi di \texttt{config.yaml} fino agli oggetti \texttt{ConnectorResult} prodotti dai tool esterni, è modellata come classe Pydantic con validatori espliciti. I \texttt{model\_validator} e i \texttt{field\_validator} applicano le invarianti nel momento della costruzione dell'oggetto: se nel \texttt{config.yaml} un tool esterno è marcato \texttt{enabled~=~True} ma il campo \texttt{timeout\_seconds} è assente, la Fase~1 solleva un \texttt{ConfigurationError} con il percorso esatto del campo mancante e l'assessment non inizia. Se un connector produce un \texttt{ConnectorResult} con un campo obbligatorio mancante, Pydantic solleva un errore di validazione al momento della costruzione dell'oggetto, indicando il campo violato, prima che il dato raggiunga la logica di assessment.

I due strati hanno copertura complementare: mypy intercetta gli errori di interfaccia tra moduli prima dell'esecuzione, Pydantic intercetta i dati malformati al confine con l'esterno al momento della costruzione. Un difetto che sfugge al primo viene catturato dal secondo; la copertura combinata riduce lo spazio degli errori che possono propagarsi in silenzio verso la logica di assessment. Per chi aggiunge un test o un connector, entrambi i meccanismi sono già attivi per costruzione: i modelli Pydantic di configurazione e di risultato sono già frozen e tipizzati, e la conformità ai type hints è verificabile con \texttt{mypy src/} senza configurazione aggiuntiva.


\subsection{Indicatori di Qualità dell'Ingegneria di Produzione}\label{subsec:production-quality}

Tre verifiche ulteriori riguardano la riproducibilità e la portabilità del tool come artefatto distribuibile, indipendentemente dall'output dell'assessment.

La prima è la copertura della documentazione interna: tutti i 292 simboli pubblici della codebase presentano una docstring, verificato tramite ispezione dell'\gls{AST}\footnote{L'\emph{Abstract Syntax Tree} è la rappresentazione strutturata del codice sorgente usata dai compilatori e dagli analizzatori statici. Ispezionare l'AST permette di estrarre i simboli pubblici definiti e verificare la presenza della docstring associata senza eseguire il programma.}. In un tool progettato per essere esteso, la documentazione dei contratti pubblici è il meccanismo che permette a chi aggiunge un test di determinare cosa può chiamare e con quali aspettative di comportamento.

La seconda è la riproducibilità della build: due esecuzioni consecutive del comando \texttt{hatch~build~-{}-target~wheel}\footnote{\texttt{hatch} è il build backend del progetto. Il comando produce un file \texttt{.whl} (\emph{wheel}), il formato standard di distribuzione per i package Python: installabile con \texttt{pip} senza compilazione, non dipende dalla piattaforma e non richiede estensioni native. Il tag \texttt{py3-none-any} nel nome lo conferma.} producono un artefatto con hash SHA-256 identico. Una build non deterministica introdurrebbe variabilità tra versioni nominalmente identiche, rendendo impossibile garantire che il tool in produzione corrisponda esattamente all'artefatto verificato.

La terza riguarda l'installabilità in ambienti puliti. Il pacchetto generato dalla build può essere installato in un ambiente virtuale Python privo di dipendenze preinstallate con \texttt{pip~install}; al termine, \texttt{apiguard~version} restituisce \texttt{0.1.0} e il tool è operativo. La verifica conferma che l'installazione non richiede dipendenze di sistema non dichiarate e che il comportamento del tool non dipende da pacchetti preinstallati nell'ambiente dell'esecutore, condizione descritta nella Sezione~\ref{subsec:zero-state}. I risultati delle verifiche sono riportati nella Tabella~\ref{tab:verdetto-release}.

\input{assets/tables/5-verdetto-release.tex}








% ─────────────────────────────────────────────────────────────────────────────
\section{Copertura Funzionale e Idempotenza dell'Assessment}%
\label{sec:coverage-idempotenza}
% ─────────────────────────────────────────────────────────────────────────────

I test che creano risorse sul target, le interrogano e poi le eliminano introducono dipendenze di stato che, se mal gestite, producono risultati diversi tra un'esecuzione e la successiva: la prima modifica il target, la seconda trova uno stato alterato. Dimostrare empiricamente che due esecuzioni successive producono risultati identici è il modo concreto per verificare che la pipeline non lasci effetti collaterali osservabili e che le proprietà di teardown e riproducibilità, discusse nelle Sezioni~\ref{subsec:teardown} e~\ref{subsec:reproducibility}, si comportino nel modo dichiarato su un target reale.

Le due esecuzioni sono state condotte in sequenza nella stessa giornata contro lo stesso testbed, senza alcun intervento manuale tra l'una e l'altra. Questa sezione analizza i risultati per dominio (Sezione~\ref{subsec:analisi-domini}) e verifica la stabilità dell'esecuzione e il corretto cleanup delle risorse (Sezione~\ref{subsec:idempotenza-teardown}).


\subsection{Analisi dei Risultati per Dominio}\label{subsec:analisi-domini}

La mappa di copertura dell'assessment è illustrata nella Figura~\ref{fig:coverage-map}; il dettaglio per test, con status e finding count, è riportato nella Tabella~\ref{tab:risultati-per-test}. I 18 test attivi hanno prodotto 7~\texttt{FAIL}, 9~\texttt{PASS} e 2~\texttt{SKIP} per un totale di 98 finding; segue una lettura trasversale dei tre esiti.

\input{assets/figures/5-coverage-map.tex}

\input{assets/tables/5-risultati-per-test.tex}

I test eseguiti in questa sessione di validazione sono catalogati con le rispettive garanzie nella Sezione~\ref{subsec:test-catalog} e con i loro criteri di valutazione specifici nella Sezione~\ref{subsec:test-oracles}.
I 7 \texttt{FAIL} corrispondono alle condizioni configurate nel testbed descritte nella Sezione~\ref{subsec:configurazione-testbed}. Il test~1.1 da solo produce 74 dei 98 finding totali (75.5\%): quegli endpoint di Forgejo che il campo \texttt{security} della specifica marca come autenticati, ma che rispondono \texttt{2xx} a richieste prive di credenziali, sono finding corretti dal punto di vista del contratto tra specifica e comportamento osservato, come discusso nella Sezione~\ref{subsec:configurazione-testbed}. I restanti 24 finding sono distribuiti su test~0.1 (16), test~0.2 (1), test~4.1 (2), test~7.2 (1), \texttt{ext.1.5.sslyze} (1) e \texttt{ext.1.5.testssl} (3).

I 2 \texttt{SKIP} illustrano il comportamento del sistema in assenza di condizioni di applicabilità. Il contratto di \texttt{execute()}, descritto nella Sezione~\ref{sec:basetest}, stabilisce che ogni test produce sempre un \texttt{TestResult}: quando lo scope applicabile è vuoto o un prerequisito non è soddisfatto, il risultato è \texttt{SKIP} con motivazione esplicita, non un errore silenzioso. Il test~0.3 verifica che gli endpoint marcati \texttt{deprecated:~true} nella specifica restituiscano \texttt{410~Gone} o portino un header \texttt{Sunset} (RFC~8594); la specifica OpenAPI di Forgejo~14.0.3 non dichiara nessun endpoint deprecato, e il test restituisce \texttt{SKIP} con motivo documentato. Il test~1.6 verifica la configurazione del session store tramite la Admin \gls{API} di Kong; nella configurazione del testbed Kong non gestisce un session store esterno, e la condizione non è soddisfatta. In entrambi i casi uno \texttt{SKIP} con motivazione esplicita è la risposta semanticamente corretta, distinta da un \texttt{ERROR} che indicherebbe un malfunzionamento della pipeline.

I 9 \texttt{PASS} coprono le configurazioni correttamente applicate nel testbed: l'assenza di fingerprinting del gateway (test~6.2), il redirect \gls{HTTP}$\to$\gls{HTTPS} (test~1.5), la revoca corretta del token (test~1.4), l'enforcement dell'autenticazione sui test \texttt{GREY\_BOX} (test~2.1), la configurazione degli upstream Kong (test~6.4), i controlli di integrità dei dati e disponibilità (test~3.3, 4.2, 4.3), e il tool esterno \texttt{ext.0.1.nuclei} che non ha rilevato Shadow \gls{API}, confermando che la superficie documentata dalla specifica corrisponde a quella effettivamente esposta.


\subsection{Idempotenza e Validazione del Teardown}\label{subsec:idempotenza-teardown}

La Tabella~\ref{tab:idempotenza-kpi} riporta i \gls{KPI} aggregati delle due esecuzioni. I contatori di finding, gli status per-test, il codice di uscita e il \texttt{pass\_rate\_pct} sono byte-identici tra le due run: la parte deterministica del risultato non mostra alcuna variazione. Il delta sul wall-clock è di 0.67~secondi su una durata di circa 290~secondi, pari allo 0.23\% della durata totale; questa variazione rientra nella fluttuazione attesa delle latenze di rete \gls{HTTP} verso il target e non è attribuibile a comportamento non deterministico della pipeline.

\input{assets/tables/5-idempotenza-kpi.tex}

Il teardown, descritto nella Sezione~\ref{subsec:teardown}, costituisce la precondizione dell'idempotenza: senza un cleanup completo delle risorse create durante la prima esecuzione, la seconda troverebbe uno stato alterato e produrrebbe risultati diversi. La verifica condotta dopo ciascuna delle due esecuzioni ha confermato che la Fase~6 ha drenato il registro \gls{LIFO} senza lasciare risorse residue: nessun token con prefisso \texttt{"apiguard"} sull'account \texttt{thesis-admin} e nessuna repository con prefisso \texttt{"apiguard"} sull'account \texttt{user-a} erano presenti al termine di ciascuna esecuzione. Zero risorse residue su due esecuzioni indipendenti è la conferma empirica che la pipeline si comporta nel modo dichiarato in presenza di stato reale.






% ─────────────────────────────────────────────────────────────────────────────
\section{Footprint Computazionale e Benchmarking del DAG}%
\label{sec:footprint-dag}
% ─────────────────────────────────────────────────────────────────────────────

L'integrabilità di un tool di assessment in una pipeline \gls{CI/CD} dipende anche dal suo profilo di risorse: durata dell'esecuzione, consumo di memoria e dimensione degli output determinano su quale classe di infrastruttura il tool è operativamente sostenibile. Questa sezione riporta le misurazioni ottenute su due run di validazione.

Le metriche di sistema riportate nella Tabella~\ref{tab:performance-baseline} sono state ottenute tramite \texttt{/usr/bin/time -v} applicato al processo \texttt{apiguard run}. Il \emph{wall-clock time} è il tempo reale trascorso dall'avvio alla terminazione, comprensivo di attese di rete, I/O e scheduling del sistema operativo. Lo \emph{user time} e il \emph{system time} misurano il tempo CPU consumato rispettivamente in user space (codice Python e librerie) e in kernel space (system call, gestione dei processi figli): entrambi sono prodotti da \texttt{/usr/bin/time -v} e la loro somma dà il tempo CPU totale effettivo.

\input{assets/tables/5-performance-baseline.tex}


\subsection{Regime di Esecuzione: Dominato dalla Latenza di Rete}\label{subsec:regime-esecuzione}

La relazione tra user time, system time e wall-clock rivela il regime di esecuzione. Su 290\,s di wall-clock, il processo ha consumato circa 35\,s di tempo CPU in user space e 41\,s in kernel space: la somma è 76\,s, il 26\% del totale. I restanti 214\,s il processo li ha trascorsi in attesa delle risposte \gls{HTTP} dal target, del completamento dei processi esterni (nuclei, testssl.sh) e dell'I/O su disco per la scrittura dell'evidence store. Il collo di bottiglia è la latenza di rete verso il target; ottimizzazioni al codice Python avrebbero un impatto marginale sul wall-clock totale. La riduzione più significativa del tempo di esecuzione verrebbe dalla parallelizzazione dei test privi di dipendenze reciproche, direzione discussa come sviluppo futuro nella Sezione~\ref{subsec:roadmap-operativa}. Con l'architettura sequenziale attuale, il profilo misurato rappresenta il tempo di esecuzione caratteristico per un assessment di questa dimensione.


Il picco di memoria di 287--295\,MB e il footprint su disco di 4.5\,MB \footnote{\texttt{apiguard\_report.json} 2.26\,MB, \texttt{assessment\_report.html} 2.0\,MB, \texttt{evidence.json} 254\,KB} sono compatibili con le limitazioni tipiche dei sistemi di \gls{CI/CD}, che operano generalmente su macchine con vincoli di memoria nell'ordine dei gigabyte. La relazione tra topologia del \gls{DAG} e footprint è discussa nella Sezione~\ref{subsec:topologia-dag}.


\subsection{Topologia del DAG e Implicazioni sull'Ordinamento dell'Esecuzione}\label{subsec:topologia-dag}

Eseguire tutti i test in un singolo batch senza \gls{DAG} sarebbe la struttura più semplice: meno codice, nessun overhead di scheduling. Il problema è la correttezza dei risultati in presenza di dipendenze tra test.

Alcuni test richiedono che altri test abbiano già prodotto un certo stato nel \texttt{TestContext} prima di poter essere eseguiti. Nell'assessment di validazione, i test~1.4 (revoca del token) e~2.1 (enforcement \gls{RBAC}) dipendono entrambi da un token valido prodotto dal test~1.1: senza quel prerequisito, i due test produrrebbero risultati che dipendono dall'ordine di esecuzione e non dal comportamento effettivo del target. Il \gls{DAG} risolve questa dipendenza per costruzione: i test con \texttt{depends\_on} dichiarato vengono schedulati in una fase successiva, e non possono mai essere avviati prima che il predecessore abbia prodotto un risultato.

La Tabella~\ref{tab:dag-topology} riporta la topologia risultante: 16 test senza dipendenze formano la Phase~A; 1.4 e~2.1 vengono schedulati in Phase~B e non possono mai essere avviati prima che il test~1.1 abbia prodotto un risultato.

\input{assets/tables/5-dag-topology.tex}

Il \gls{DAG} di validazione è sparso: su 18 nodi, solo 2 hanno dipendenze dichiarate, entrambe convergenti sullo stesso predecessore. Con questo grado di connessione il calcolo dell'ordinamento topologico è istantaneo; la giustificazione del componente non è la complessità del caso attuale, ma la scalabilità: quando la suite crescerà a coprire più domini con dipendenze incrociate tra test di aree diverse, il grafo diventerà più denso e garantire la correttezza dell'ordinamento senza un meccanismo formale diventerà progressivamente più difficile. La validazione condotta non dimostra solo che le due dipendenze dichiarate sono gestite correttamente, ma che l'infrastruttura di scheduling è funzionante e pronta per accogliere grafi più complessi, come descritto tra gli sviluppi futuri nella Sezione~\ref{subsec:roadmap-operativa}, senza modifiche architetturali al core.

La topologia ha una relazione diretta con il footprint di memoria. Con 16 test in esecuzione sequenziale nella Phase~A, l'\texttt{EvidenceStore} mantiene aperto un file \texttt{.jsonl} per volta: il picco di memoria non è determinato dal numero di test paralleli ma dalla dimensione del singolo audit trail; i 287--295\,MB misurati sono il profilo effettivo di questa esecuzione. La transizione verso l'esecuzione parallela dei test all'interno della stessa fase, discussa come sviluppo futuro nella Sezione~\ref{subsec:roadmap-operativa}, richiederà una rivalutazione di questo profilo: più file \texttt{.jsonl} aperti concorrentemente comportano un footprint proporzionalmente maggiore, da misurare prima di fissare i requisiti di memoria per deployment \gls{CI/CD}.









% ─────────────────────────────────────────────────────────────────────────────
\section*{Sintesi della Validazione}
\addcontentsline{toc}{section}{Sintesi della Validazione}
% ─────────────────────────────────────────────────────────────────────────────

Su un target reale con specifica \gls{OAS} nativa, APIGuard Assurance v0.1.0 ha eseguito 18 test distribuiti su 7 domini di sicurezza, prodotto 98 finding, restituito risultati byte-identici su due run indipendenti, drenato tutte le risorse transitorie senza residui, e completato l'assessment in circa 4 minuti e 50 secondi. I quattro livelli di analisi di questo capitolo formano una gerarchia di precondizioni: ciascuno è condizione di interpretabilità del successivo, dal testbed che stabilisce le condizioni di osservazione fino al profilo computazionale che ne qualifica l'integrabilità operativa.

La validazione empirica misura comportamenti osservabili su un target reale; i limiti strutturali del paradigma adottato e le tensioni architetturali che ne derivano sono l'oggetto del Capitolo~\ref{chap:discussion}.

